# ================================================================
# CONFIGURACIÓN DEL EVALUADOR DE SITIOS WEB GUBERNAMENTALES
# ================================================================
# Copiar este archivo como .env y configurar los valores apropiados

# Database Configuration
# IMPORTANTE: Cambiar credenciales en producción
DATABASE_URL=postgresql://gob_admin:gob_dev_2024@localhost:5432/gob_evaluator
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# Redis Configuration
REDIS_URL=redis://localhost:6379/0

# Application Configuration
APP_NAME=GOB.BO Evaluator
APP_VERSION=1.0.0
ENVIRONMENT=development
# CRÍTICO: Generar clave secreta segura para producción (mínimo 32 caracteres)
# Ejemplo: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-secret-key-change-in-production-min-32-characters
DEBUG=true
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# API Configuration
API_V1_PREFIX=/api/v1
MAX_CONCURRENT_EVALUATIONS=5

# Crawler Configuration
CRAWLER_USER_AGENT=GobBoEvaluator/1.0 (+https://evaluador.gob.bo)
CRAWLER_DELAY=2
CRAWLER_MAX_DEPTH=3
CRAWLER_TIMEOUT=30
CRAWLER_MAX_PAGES=100

# NLP Configuration
NLP_MODEL_NAME=dccuchile/bert-base-spanish-wwm-cased
NLP_CACHE_DIR=./models_cache

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Evaluation Configuration
WCAG_MIN_SCORE=70.0
DS3925_MIN_SCORE=70.0

# Email Configuration (Gmail SMTP para 2FA)
# Para obtener la contraseña de aplicación de Gmail:
# 1. Activar verificación en 2 pasos en tu cuenta Google
# 2. Ir a https://myaccount.google.com/apppasswords
# 3. Generar una contraseña para "Otra aplicación (nombre personalizado)"
MAIL_USERNAME=tu-correo@gmail.com
MAIL_PASSWORD=xxxx-xxxx-xxxx-xxxx
MAIL_FROM=noreply@evaluador.gob.bo
MAIL_FROM_NAME=Evaluador GOB.BO
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_TLS=true
MAIL_SSL=false
